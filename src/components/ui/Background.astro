---
export interface Props {
  isDark?: boolean;
}

const { isDark = false } = Astro.props;
---

<div class:list={['absolute inset-0', { 'bg-green-500 dark:bg-transparent': isDark }]}>
  <slot />
</div>
